{
  "name": "remasteria",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "gallery:build": "node scripts/build-gallery.mjs",
    "gallery:check": "node scripts/check-gallery.mjs",
    "gallery:rebuild": "node scripts/rebuild-gallery.mjs",
    "gallery:migrate": "node scripts/migrate-gallery.mjs",
    "studio": "node tools/studio/server.mjs",
    "predev": "npm run gallery:build",
    "dev": "next dev",
    "prebuild": "npm run gallery:build",
    "build": "next build",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.check.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "lucide-react": "latest",
    "next": "latest",
    "open": "^10.2.0",
    "react": "latest",
    "react-dom": "latest",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "eslint": "latest",
    "eslint-config-next": "latest",
    "typescript": "latest",
    "vitest": "latest"
  },
  "overrides": {
    "postcss": "^8.5.10",
    "sharp": "$sharp"
  }
}
